import type { Component, Setter } from 'solid-js';

import Styles from './index.module.scss';

import type { ClockConfig } from '@/types/config';

const Config: Component<{ SetConfig: Setter<ClockConfig> }> = (Props) => {
  return (
    <div></div>
  );
};